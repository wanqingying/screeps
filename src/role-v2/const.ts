export enum role_stat {
  idle = "idle",

  restore = "restore",

  harvest = "harvest",

  build = "build",

  repair = "repair",

  upgrade = "upgrade",

  drop = "drop",
}

export enum ret_code {
  err_no_target = -1,
}
